<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bhadoria.abhinav.presentation">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="bhadoria.abhinav.presentation.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="bhadoria.abhinav.presentation.ui.splash.SplashActivity"
            android:theme="@style/AppTheme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name="bhadoria.abhinav.presentation.ui.pullrequestviewer.PullRequestViewerActivity" />

    </application>

</manifest>
